# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, e.g. receiver/prometheus
component: exporter/exporterhelper

# A brief description of the change. Surround your text with quotes ("...") if it contains special characters.
note: Stabilize `AddMetricsObserver` in exporterhelper to allow exporters to observe metrics emitted by the exporter helper.

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
issues: [11783]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the changelog.
# Use pipe (|) for multiline entries.
subtext: |
  The `AddMetricsObserver` function has been stabilized and is no longer experimental.
  This allows exporters to register observers for metrics emitted by the exporter helper,
  enabling better observability and monitoring of exporter behavior.

# If your change doesn't affect end users or the exported elements of any package,
# you should instead start your pull request title with [chore] or use the 'chore' change type.
# One of 'alpha', 'beta', 'stable'
change_status: stable

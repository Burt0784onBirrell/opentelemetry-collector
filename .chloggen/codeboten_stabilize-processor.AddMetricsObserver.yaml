# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. otlpreceiver)
component: processor

# A brief description of the change. Surround your text with quotes ("text") if it contains special characters.
note: Stabilize processor.AddMetricsObserver by removing the alpha gate

# One or more tracking issues related to the change
issues: [11208]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the changelog.
# Use pipe (|) for multiline entries.
subtext: |
  The processor.AddMetricsObserver feature gate has been stabilized and
  the alpha gate has been removed. This functionality is now enabled by
  default for all processors that implement the MetricsObserver interface.
